<import src="./new.wxml" />
<template name="itemDetail">
  <view>
    <text bindtap='getContent' id="{{title}}" class=''>{{title}}</text>
    <block wx:if="{{new_tag}}">
      <template is="new" data="{{new:true}}" />
    </block>
  </view>
</template>